# F-Field Release Data Backup Checklist

이 문서는 Play Store 출시 전 데이터 백업/복원 동작을 검증하기 위한 체크리스트입니다.

## 현재 데이터 저장 구조

- 앱 기록과 프로젝트는 기기 내부 저장소(IndexedDB/localforage)에 저장된다.
- 기록에 첨부한 사진은 현재 기록 데이터 안에 포함되어 함께 저장된다.
- 앱을 삭제하거나 앱 데이터를 초기화하면 내부 저장 데이터가 사라질 수 있다.
- 기기 교체, 앱 삭제, 재설치 전에는 반드시 프로젝트 저장 또는 데이터 백업을 해야 한다.

## 사용자가 쓸 수 있는 백업 경로

### 현재 프로젝트 저장

- 위치: `기록 관리` 탭 하단 `프로젝트 저장`
- 결과: 현재 프로젝트의 모든 기록이 하나의 `.geojson` 파일로 저장된다.
- 포함 대상: 도형, 기록명, 메모, 스타일, 첨부 사진
- 복원 방식: `불러오기`로 해당 `.geojson` 파일 선택
- 복원 결과: 같은 이름의 프로젝트가 이미 있으면 새 프로젝트명에 `(2)`, `(3)`이 붙는다.

### 전체 데이터 백업

- 위치: `프로젝트 관리` 탭 하단 `데이터 백업`
- 결과: 모든 프로젝트가 각각 `.geojson`으로 묶인 `.zip` 파일로 저장된다.
- 포함 대상: 각 프로젝트의 도형, 기록명, 메모, 스타일, 첨부 사진
- 복원 방식: `불러오기`로 백업 `.zip` 파일 선택
- 복원 결과: 프로젝트 단위로 다시 추가된다.

### 선택 기록 저장

- 위치: `기록 관리` 탭 상단 점 세 개 메뉴 `선택 저장`
- 결과: 선택한 기록을 GeoJSON/SHP/GPX 중 선택한 형식으로 저장한다.
- 주의: 앱으로 다시 완전 복원할 목적이면 GeoJSON을 우선 사용한다.
- SHP/GPX는 외부 GIS 호환 목적이며, 앱 전용 스타일/사진 정보 보존 범위가 제한될 수 있다.

### 사진 저장

- 위치: 기록 상세 사진 보기 화면의 다운로드 버튼
- 결과: 선택한 사진 한 장을 기기에 저장한다.
- 주의: 개별 사진 저장은 백업이 아니라 사진 추출 기능이다.

## 출시 전 필수 검증 시나리오

### 1. 현재 프로젝트 저장/복원

- [ ] 점 기록 1개 생성
- [ ] 선 기록 1개 생성
- [ ] 면 기록 1개 생성
- [ ] 사진 첨부 기록 1개 생성
- [ ] 각 기록의 색상/선굵기/투명도/마커 스타일 변경
- [ ] `기록 관리 > 프로젝트 저장`으로 `.geojson` 저장
- [ ] `프로젝트 관리 > 초기화`로 데이터 삭제
- [ ] 저장한 `.geojson`을 `불러오기`
- [ ] 프로젝트가 새로 생성되는지 확인
- [ ] 점/선/면 위치와 모양이 유지되는지 확인
- [ ] 기록명, 메모, 스타일이 유지되는지 확인
- [ ] 첨부 사진이 열리는지 확인
- [ ] 첨부 사진 다운로드가 되는지 확인

### 2. 전체 데이터 백업/복원

- [ ] 프로젝트 2개 이상 생성
- [ ] 각 프로젝트에 기록 2개 이상 생성
- [ ] 하나 이상의 기록에 사진 첨부
- [ ] `프로젝트 관리 > 데이터 백업`으로 `.zip` 저장
- [ ] `프로젝트 관리 > 초기화`로 데이터 삭제
- [ ] 저장한 `.zip`을 `불러오기`
- [ ] 프로젝트 개수가 복원되는지 확인
- [ ] 각 프로젝트의 기록 개수가 맞는지 확인
- [ ] 사진 포함 기록이 정상 표시되는지 확인
- [ ] 프로젝트 전환 후 지도 표시가 정상인지 확인

### 3. 앱 삭제/재설치 전후 확인

- [ ] 앱 삭제 전 `데이터 백업`으로 `.zip` 저장
- [ ] 저장 파일이 기기 `다운로드` 또는 사용자가 찾을 수 있는 위치에 있는지 확인
- [ ] 앱 삭제 후 재설치
- [ ] 앱 내부 기록이 비어 있는지 확인
- [ ] 백업 `.zip`을 `불러오기`
- [ ] 프로젝트와 사진이 복원되는지 확인

### 4. Android 저장 시트 확인

- [ ] 단일 프로젝트 `.geojson` 저장 위치 선택 가능
- [ ] 전체 백업 `.zip` 저장 위치 선택 가능
- [ ] 선택 기록 저장 위치 선택 가능
- [ ] 사진 저장 위치 선택 가능
- [ ] Google Drive, 카카오톡 등 다른 앱 공유도 가능
- [ ] `내 파일/다운로드`처럼 기기 내부 저장 위치에도 저장 가능

## 사용자 안내 문구 초안

앱의 기록과 사진은 기본적으로 사용자의 기기 내부에 저장됩니다. 앱을 삭제하거나 앱 데이터를 초기화하면 저장된 기록이 사라질 수 있으므로, 기기 변경이나 앱 삭제 전에는 `프로젝트 관리 > 데이터 백업`으로 백업 파일을 저장해 주세요.

개별 프로젝트만 보관하려면 `기록 관리 > 프로젝트 저장`을 사용할 수 있습니다. 앱으로 다시 불러와야 하는 백업 파일은 GeoJSON 형식을 권장합니다.

## 출시 전 판단 기준

- 현재 프로젝트 저장/복원이 사진 포함 기록까지 성공해야 한다.
- 전체 데이터 백업/복원이 프로젝트 2개 이상에서 성공해야 한다.
- 앱 삭제 후 재설치 상황에서 백업 파일로 복원할 수 있어야 한다.
- 사용자가 저장한 파일을 기기에서 찾을 수 있어야 한다.
